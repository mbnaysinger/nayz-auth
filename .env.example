# Configurações do Banco de Dados
DATABASE_URL=postgres://nayz:nayzpassword@localhost:5432/nayz_auth?sslmode=disable

# Configurações do Servidor
PORT=8080

# Conexão com Redis
REDIS_URL=localhost:6379

# Servidor SMTP (Mailpit Local)
SMTP_HOST=localhost
SMTP_PORT=1025

# CORS: origens permitidas separadas por vírgula. Vazio = "*" (permissivo, dev).
# CORS_ALLOWED_ORIGINS=http://localhost:5173

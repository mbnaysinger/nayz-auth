# Copie para .env — valores de BUILD do frontend (públicos, vão para o bundle JS).
# Não há segredos aqui; segredos ficam em env/*.env (chmod 600).
VITE_API_BASE_URL=https://auth.seudominio.com/api/v1
VITE_NAYZ_AUTH_APP_ID=<uuid da aplicação no nayz-auth>
